# @opencodehub/pack

Deterministic code-pack generator producing the 9-item BOM (manifest, skeleton, file-tree, deps, ast-chunks, xrefs, embeddings-sidecar, findings, licenses). See `src/types.ts` for the contract types (`PackManifest`, `BomItem`, `PackPins`, `DeterminismClass`, `PackOpts`).
