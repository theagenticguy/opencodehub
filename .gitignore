node_modules/
dist/
.tsbuildinfo
*.tsbuildinfo

# Python (eval package)
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

# mise / local overrides
mise.local.toml
.env
.env.local

# Build artifacts
*.log
.DS_Store
coverage/

# Repo-local state (not committed)
.codehub/

# Test fixtures rendered at runtime
examples/fixtures/**/.codehub/

# Local agent / planning scratch space
# .erpaval/sessions/ is per-session ephemeral state (intake, explore,
# research, lessons YAML); .erpaval/solutions/ + specs/ + INDEX.md are
# durable and stay committed.
.erpaval/sessions/
.claude/settings.local.json
.claude/worktrees/
.handoff/

# Astro build cache
packages/docs/.astro/
