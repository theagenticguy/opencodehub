name: Pages

on:
  push:
    branches: [main]
    paths:
      - "packages/docs/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    # mise-action@v4 exports NODE_ENV=development by default, which makes
    # Astro ship the dev-mode Search stub ("Search is only available in
    # production builds") instead of wiring Pagefind. Force production.
    env:
      NODE_ENV: production
    steps:
      - uses: actions/checkout@v6
      - uses: jdx/mise-action@v4
      # NOTE: --ignore-scripts removed so sharp's native binary download
      # and Playwright's chromium install (via rehype-mermaid) are allowed.
      - run: pnpm install --frozen-lockfile
      # rehype-mermaid renders ```mermaid fences to inline SVG at build
      # time using Playwright chromium. --with-deps installs the apt
      # libraries Playwright needs on ubuntu-latest. Do not use --with-deps
      # on dev machines.
      - run: pnpm --filter @opencodehub/docs exec playwright install chromium --with-deps
      - run: pnpm -F @opencodehub/docs build
      - uses: actions/upload-pages-artifact@v5
        with:
          path: packages/docs/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
